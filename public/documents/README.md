# Documenti trasparenza

Deposita qui i PDF referenziati in `src/config/loans.ts` (`transparencyDocuments`):

- `guida-credito-consumatori.pdf`
- `guida-abf.pdf`
- `tegm.pdf`

Quando i file sono presenti, imposta `available: true` per ciascun documento in `src/config/loans.ts`.
